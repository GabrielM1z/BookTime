import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 12,
  },
});

export default commonStyles;